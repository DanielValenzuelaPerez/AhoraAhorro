<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahoraahorro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AhoraAhorro">
        <activity android:name=".ArqueoActivity" android:label="Arqueo"></activity>
        <activity android:name=".AgregarActivity" android:label="Nuevo movimiento"/>
        <activity android:name=".AjustesActivity" android:label="Ajustes"/>
        <activity android:name=".MainActivity" android:label="¡Ahora Ahorro!">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>